services:
  app:
    ports:
      - "2345:2345"
    security_opt:
      - "seccomp:unconfined"
    command: ["dlv", "--listen=:2345", "--headless=true", "--api-version=2", "--accept-multiclient", "exec", "/app/main"]